# Architecture Decision Records

Accepted ADRs belong to the frozen architecture baseline. This bootstrap repository does not contain the full historical ADR text because the exact approved source files were not available in the active workspace.

Add canonical ADR files here before treating the repository as a complete baseline.
