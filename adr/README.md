# Architecture Decision Records

The complete approved ADR set was not available during repository bootstrap.

Do not fabricate ADR content. Import each approved ADR as a separate file using the naming convention:

```text
ADR-###-short-title.md
```
